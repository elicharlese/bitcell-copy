pragma solidity ^0.6.0

contract Testing_Demand {
    function set_demand_line() private {
    //Set demand line
    }

    function successful_demand_tests() private {
    //Run a wave of tests successfully
    }

    function error_demand_tests() private {
    //Catch all errors from waves of tests
    }
}

contract Begin_Accumulation {
    function final_demand_test() private {
    //Final Demand Test
    }

    function trigger_selling_climax() private {
    //Trigger Selling Climax
    }
}