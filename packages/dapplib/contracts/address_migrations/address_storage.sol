pragma solidity ^0.6.0

contract Store_Addresses {
    function store_new_address() public {

    }
}

contract Search_Addresses {
//Will comb through the historical record of addresses to see what has been used in the past and whem
    function search_address_bank() {
        if address = current_address {
            msg.sender["Found it:" current_address]
        }
    }
}