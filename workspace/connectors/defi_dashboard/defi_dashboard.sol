pragma solidity ^0.6.0

contract Blockchain_Connector {
    function gather_data_from_bitcell() private {
        //Gather data from bitcell for representation
        uint256 = addresses[];
        uint256 = hash.addresses;

    }
    function format_data_from_bitcell() private {
        //Format the data for transfer
        profit = 
        loss = 
        total_transactions = 
        exchanges = {"Kucoin" : url, "Binance" : url,}
    }
    function {
        //
    }
    function {
        //
    }
    function {
        //Send data to defi dashboard
        msg.sender[]
    }
    function alert_the_regulator() private {
        //
    }
    function reset_data_update_timer() private {
        //
    }
}